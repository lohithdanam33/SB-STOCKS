# MongoDB connection string (MongoDB Atlas, local mongod, or any MongoDB provider)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/tronx

# Secret used to sign JWT session cookies — use a long random string in production
JWT_SECRET=sb-stocks-trading-secret-key-2026

# Finnhub API key (free tier works for live quotes; historical candles require a paid plan)
FINNHUB_API_KEY=your_finnhub_key_here
